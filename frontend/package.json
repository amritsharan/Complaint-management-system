{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Complaint management system frontend",
  "scripts": {
    "dev": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
